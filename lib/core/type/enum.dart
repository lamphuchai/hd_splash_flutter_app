enum StatusType { init, loading, loaded, error }

enum LoadQualityType { raw, full, regular, small, thumb }

enum LoadType { refresh, page }

enum AppAction { deleteCollection, removePhotoFromCollection,updateCollection }
